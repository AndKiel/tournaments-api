en:
  doorkeeper:
    errors:
      messages:
        invalid_resource_owner: 'The provided user credentials are not valid, or user cannot be found.'
  dry_validation:
    errors:
      after_now?: 'must be a future date'
      confirmed?:
        arg:
          default: 'must match %{key}'
      email?: 'is not a valid e-mail address'
      unique?: 'has already been taken'
      rules:
        result_values:
          size?: 'amount must match the Tournament result names'
  pundit:
    default: 'You cannot perform this action.'
    tournament_policy:
      start?: 'You cannot start a tournament before its set date.'
