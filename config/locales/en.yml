en:
  doorkeeper:
    errors:
      messages:
        invalid_resource_owner: 'The provided user credentials are not valid, or user cannot be found.'
  errors:
    messages:
      invalid_email: 'is not a valid e-mail address'
      future_date: 'must be a future date'
    attributes:
      result_values:
        invalid: 'provide all values, only integers are accepted'
  pundit:
    default: 'You cannot perform this action.'
    tournament_policy:
      start?: 'You cannot start a tournament before its set date.'
